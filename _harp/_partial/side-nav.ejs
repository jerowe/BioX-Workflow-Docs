    <nav class="navbar-default navbar-static-side" role="navigation">
        <div class="sidebar-collapse">
            <ul class="nav metismenu" id="side-menu">
                <li class="nav-header">
                <div class="logo-element">
                    BioX
                </div>
                </li>
                    <%
                        _.forEach(pages, function(key, value){
                            _.forEach(key, function(ikey, slug){ %>
                            <li>
                                <a href="/<%= slug %>"><i class="fa fa-flask"></i> <span
                                        class="nav-label"> <%= ikey['title'] %></span></a>
                            </li>
                    <% }); %>
                    <% }); %>
            </ul>
        </div>
    </nav>
